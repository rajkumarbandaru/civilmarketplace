package com.civileng.marketplace.tenant.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AcmeAccountKeyRepository extends JpaRepository<AcmeAccountKey, String> {
}
