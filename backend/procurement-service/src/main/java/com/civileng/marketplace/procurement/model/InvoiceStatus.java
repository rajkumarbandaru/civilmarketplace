package com.civileng.marketplace.procurement.model;

/** MATCHED and EXCEPTION are the three-way match's verdict; APPROVED and REJECTED the buyer's decision. */
public enum InvoiceStatus {
    MATCHED, EXCEPTION, APPROVED, REJECTED
}
